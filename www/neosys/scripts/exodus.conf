<Directory "/var/www/html/exodus">
    #allow .htaccess to redirect/rewrite xhttp.asp to xhttp.php
    #requires "apache2 a2enmod rewrite && service apache2 reload"
    AllowOverride All
</Directory>
