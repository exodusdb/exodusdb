//generated by exodus "compile quickdexer"
//#ifndef EXODUSDLFUNC_QUICKDEXER_H
#define EXODUSDLFUNC_QUICKDEXER_H

//a member variable/object to cache a pointer/object for the shared library function
//ExodusFunctorBase efb_quickdexer;
class efb_quickdexer : public ExodusFunctorBase
{
public:

efb_quickdexer(MvEnvironment& mv) : ExodusFunctorBase("quickdexer", "exodusprogrambasecreatedelete_", mv) {}

efb_quickdexer& operator=(const var& newlibraryname) {
        closelib();
        libraryname_=newlibraryname.toString();
        return (*this);
}

//a member function with the right arguments, returning a var or void
var operator() (in filename, in mode, in v3, in v4)
{

 //first time link to the shared lib and create/cache an object from it
 //passing current standard variables in mv
 //first time link to the shared lib and create/cache an object from it
 //passing current standard variables in mv
 //if (efb_getlang.pmemberfunction_==NULL)
 // efb_getlang.init("getlang","exodusprogrambasecreatedelete_",mv);
 if (this->pmemberfunction_==NULL)
  this->init();

 //define a function type (pExodusProgramBaseMemberFunction)
 //that can call the shared library object member function
 //with the right arguments and returning a var or void
 typedef var (ExodusProgramBase::*pExodusProgramBaseMemberFunction)(in,in,in,in);

 //call the shared library object main function with the right args,
 // returning a var or void
 //return CALLMEMBERFUNCTION(*(efb_quickdexer.pobject_),
 //((pExodusProgramBaseMemberFunction) (efb_quickdexer.pmemberfunction_)))
 // (mode);
 return CALLMEMBERFUNCTION(*(this->pobject_),
 ((pExodusProgramBaseMemberFunction) (this->pmemberfunction_)))
  (filename,mode,v3,v4);

}

var operator() () {
 var filename_in;
 var mode_in;
 var v3_in;
 var v4_in;
 return operator()(filename_in, mode_in, v3_in, v4_in);
}

var operator() (in filename) {
 var mode_in;
 var v3_in;
 var v4_in;
 return operator()(filename, mode_in, v3_in, v4_in);
}

var operator() (in filename, in mode) {
 var v3_in;
 var v4_in;
 return operator()(filename, mode, v3_in, v4_in);
}

var operator() (in filename, in mode, in v3) {
 var v4_in;
 return operator()(filename, mode, v3, v4_in);
}

};
efb_quickdexer quickdexer{mv};
//#endif
