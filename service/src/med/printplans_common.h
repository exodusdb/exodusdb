//only put small vars here since it remains in memory after program termination
//is not a concern for c++

	#define creditnote pseudo.a(21) == 4
	#define wkly index(mthly,"W",1)

	pp.grpstot.redim(3);
	pp.tx2.redim(20);
	pp.damt.redim(7);
	pp.narr.redim(2);
	pp.addtx.redim(2);
	pp.doccompanycode.redim(2);
	pp.doccompany.redim(2);
	pp.dd.redim(2);
	pp.did.redim(2);
	pp.dtext.redim(2, 50);
	pp.pn.redim(2);
	pp.np.redim(2);
	nbsp = "&nbsp;";
	certonly2 = 1;