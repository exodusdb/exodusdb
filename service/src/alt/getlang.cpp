#include <exodus/library.h>
libraryinit()

//stub to acquire language text for a given program and language code

function main(in origprogname, in languagecode0, in origdatatype, io languagefile, io lang) {
	false && origprogname && languagecode0 && origdatatype && languagefile;
	lang="";
	return 1;
}

libraryexit()
