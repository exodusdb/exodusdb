#include <exodus/library.h>
libraryinit()

// OBSOLETE replaced by stop(); everywhere

func main() {
	// printl("Goodbye");
	// exit(0);
//	throw ExoStop("Logoff");
	stop();
	//std::unreachable();
}

}; // libraryexit()
