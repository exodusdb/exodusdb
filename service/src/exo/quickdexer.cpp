#include <exodus/library.h>
libraryinit()

function main(in filename, in mode, in v3, in v4) {
	// TODO OBSOLETE
	// call quickdexer("MATERIALS", "R", 1, "");
	false || filename || mode || v3 || v4;
	return 0;
}

libraryexit()
