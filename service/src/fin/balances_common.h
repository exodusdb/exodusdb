//

	#define maxlevs 50
	#define nbudgets 10

//dim bals2(maxlevs)
	maxcols = 100;
	if (ba.ledgern.unassigned()) {;
	 ba.ledgern = "";
	 ba.infs.redim(maxcols, 14);
	 ba.bals.redim(50);
	 ba.tots.redim(50);
	 ba.nbals.redim(50);
	 ba.foreigns.redim(50);
	 ba.cht.redim(50);
	 ba.coltit.redim(10);
	 ba.balfile.redim(100);
	 ba.tits.redim(100);
	 ba.bals2.redim(50);
	   };