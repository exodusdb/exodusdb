#!/bin/bash
set -x
set -e
:
: Generate all headers first
: ==========================
:
compile alt exo sys {HS}
:
: Compile
: =======
:
compile alt exo sys
:
: Finished
: ========
:
duration=$SECONDS
: "$(($duration / 60)) minutes and $(($duration % 60)) seconds elapsed."
:
set +x
